export enum RoomStatusEnum {
  READY = "ready",
  ING = "ing",
  FINISH = "finish",
}
export enum RoomUpdateEnum {
  JOIN = "join",
  EXIT = "exit",
}
export enum RoomResponseEnum {
  CREATE = "create",
  UPDATE = "update",
  CLOSE = "close",
}
